# GrammarComposer — Python dependencies
# Install with: pip install -r requirements.txt

# ── Core musical libraries ──────────────────────────────────────────────────
abjad>=3.19
music21>=9.1

# ── Numerical / data ────────────────────────────────────────────────────────
numpy>=1.24
matplotlib>=3.7

# ── GUI ─────────────────────────────────────────────────────────────────────
# tkinter is included with Python on Windows and macOS.
# On Ubuntu/Debian: sudo apt install python3-tk
Pillow>=10.0          # image display inside GUI (PNG preview)

# ── MIDI ────────────────────────────────────────────────────────────────────
mido>=1.3             # MIDI read/write used by analysis modules

# ── Optional (analytics export) ─────────────────────────────────────────────
# pandas>=2.0         # uncomment if using CSV/Excel analytics export
# openpyxl>=3.1       # uncomment for Excel export (.xlsx)
